# prebuilt — ROS 2 Core Dependency

Extract the ros2_core tarball directly into this directory:

```bash
tar xzf ros2-humble-x86_64.tar.gz -C prebuilt/
```

`build.sh` will auto-source `prebuilt/setup.bash`.

Contents are gitignored — only this README is tracked.
