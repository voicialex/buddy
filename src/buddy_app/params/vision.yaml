vision:
  ros__parameters:
    engine: "retinaface"        # "retinaface" | "haar"
    runtime: "onnxruntime"      # "onnxruntime" | "rknnruntime"
    face_model_dir: "models/face_emotion"
    emotion_model_path: "models/emotion"
    inference_interval_ms: 500
